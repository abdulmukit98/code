#include<bits/stdc++.h>
using namespace std;
int main()
{
    // number of rectangular plane
    int n;
    //dimension
    int a,b;
    // processiog need both surface
    scanf("%d %d %d", &n, &a, &b);
    printf("%d", n*a*b*2);

    return 0;
}
